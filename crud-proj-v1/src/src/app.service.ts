import { Injectable, Logger } from '@nestjs/common';
import { BookDto } from './app.bookstoreController';
import { BOOKS } from './mockData/book.mock';
import e = require('express');

// Node JS > Nest JS (Swagger UI)

@Injectable()
export class AppService {d}
